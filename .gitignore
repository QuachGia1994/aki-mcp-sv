node_modules/
data/
dev/
.akidevsync/
.DS_Store

# standalone-packaging build output (scripts/build/package.js) — regenerated per release, never committed
dist/

# local env overrides — .env.example is the only tracked template
.env
.env.*
!.env.example

# Cloudflare Worker local/deployment state — keep only the committed example config
.wrangler/
cloudflare/qwen-bridge-worker/wrangler.jsonc
cloudflare/qwen-bridge-worker/.dev.vars

# macOS custom folder icon resource
Icon?

# disposable visual export from /akihtmlreport, regenerated wholesale
REPORT.html
